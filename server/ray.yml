# Ray Serve multi-application configuration file
proxy_location: EveryNode

# Global HTTP options to override defaults
http_options:
  host: 0.0.0.0
  port: 8000

applications:
  - name: image_classifier
    route_prefix: /
    import_path: server:entrypoint
    runtime_env: {}
